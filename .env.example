# Copy to .env in this folder (project root) for Vite.
# Must match where the API + Socket.io server listens (default: 4000).

VITE_APP_WEB_URL=http://localhost:4000
# Production example: https://api.yourdomain.com

# Google Sign-In (from Google Cloud Console)
# VITE_GOOGLE_CLIENT_ID=
