# OWASP ZAP rules configuration
# Format:
# <pluginId> <IGNORE|INFO|WARN|FAIL> (optional text)
#
# Start strict with defaults. Add explicit overrides only after triage.
# Example:
# 10038 WARN (Content Security Policy Header Not Set)
